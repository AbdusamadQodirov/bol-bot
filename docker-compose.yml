services:
  bol-bot:
    build: .
    container_name: bol-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    # No ports exposed — bot uses outbound polling only
